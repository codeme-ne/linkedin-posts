# Environment variables for linkedin-posts (Vite)
# Copy this file to `.env` in the same folder and fill in your values.
# Only variables prefixed with VITE_ are exposed to the client by Vite.

# --- CORS Configuration (Server-side only) ---
# Comma-separated list of allowed origins
ALLOWED_ORIGINS_PROD="https://linkedin-posts-one.vercel.app"
ALLOWED_ORIGINS_DEV="http://localhost:5173,http://localhost:5174"

# --- Claude (Anthropic) ---
# Server-side API key (added to Vercel environment variables)
# Client uses /api/claude proxy route for security
CLAUDE_API_KEY=""

# --- Supabase ---
# Project URL and anon key from Supabase project settings (Public)
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
# Service role key for backend/webhooks only (Keep secret! Only for Vercel env)
SUPABASE_SERVICE_ROLE_KEY=""

# --- Stripe ---
# Payment Link URLs from Stripe Dashboard
# Legacy single payment link (kept for backward compatibility)
VITE_STRIPE_PAYMENT_LINK="https://buy.stripe.com/your-payment-link-id"
# Yearly subscription payment link (299€/year)
VITE_STRIPE_PAYMENT_LINK_YEARLY="https://buy.stripe.com/your-yearly-link-id"
# Monthly subscription payment link (29€/month)
VITE_STRIPE_PAYMENT_LINK_MONTHLY="https://buy.stripe.com/your-monthly-link-id"
# Webhook secret from Stripe Dashboard > Webhooks (starts with whsec_)
STRIPE_WEBHOOK_SECRET=""


# --- LinkedIn (optional) ---
# Use these to enable creating Draft posts via the LinkedIn REST API.
# If not provided, the app falls back to the LinkedIn share dialog.
# Access Token must include w_member_social permissions.
VITE_LINKEDIN_ACCESS_TOKEN=""
# URN format examples:
#  - Person: urn:li:person:YOUR_PERSON_ID
#  - Organization: urn:li:organization:YOUR_ORG_ID
VITE_LINKEDIN_AUTHOR_URN=""

# --- Firecrawl (Premium Extraction) ---
# API key from https://www.firecrawl.dev/
# Required for premium content extraction (JavaScript rendering, PDFs)
FIRECRAWL_API_KEY=""

# --- Feature Flags (For A/B Testing) ---
# Enable new UX design (default: false, can be overridden with rollout percentage)
VITE_ENABLE_NEW_UX=false
# Enable mobile bottom sheet for saved posts (default: false)
VITE_ENABLE_MOBILE_BOTTOM_SHEET=false
# Enable platform-specific animations (default: false)
VITE_ENABLE_PLATFORM_ANIMATIONS=false
# Enable premium features UI (default: false)
VITE_ENABLE_PREMIUM_FEATURES=false
