# =============================================================================
# SOCIAL TRANSFORMER - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit your .env file to version control!
# Only variables prefixed with VITE_ are exposed to the client by Vite.

# =============================================================================
# CORS CONFIGURATION (Server-side only)
# =============================================================================
# Comma-separated list of allowed origins for API routes
ALLOWED_ORIGINS_PROD="https://linkedin-posts-one.vercel.app,https://tranformer.social"
ALLOWED_ORIGINS_DEV="http://localhost:5173,http://localhost:5174,http://localhost:3000"

# =============================================================================
# ANTHROPIC CLAUDE API
# =============================================================================
# Get your API key from: https://console.anthropic.com/
# This is server-side only for security - client uses /api/claude proxy route
CLAUDE_API_KEY="sk-ant-api03-your-claude-api-key-here"

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project settings → API
# Public variables (safe for client-side)
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_ANON_KEY="your-supabase-anon-key-here"

# Server-side only (keep secret! Only for Vercel environment variables)
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key-here"

# =============================================================================
# STRIPE PAYMENT CONFIGURATION
# =============================================================================
# Create payment links in your Stripe dashboard
# Legacy single payment link (kept for backward compatibility)
VITE_STRIPE_PAYMENT_LINK="https://buy.stripe.com/your-payment-link-id"

# Subscription payment links
VITE_STRIPE_PAYMENT_LINK_YEARLY="https://buy.stripe.com/your-yearly-link-id"  # 299€/year
VITE_STRIPE_PAYMENT_LINK_MONTHLY="https://buy.stripe.com/your-monthly-link-id"  # 29€/month

# Stripe webhook secret for payment verification (starts with whsec_)
# Get this from Stripe Dashboard → Developers → Webhooks
STRIPE_WEBHOOK_SECRET="whsec_your-webhook-secret-here"

# =============================================================================
# LINKEDIN API (OPTIONAL - SERVER-SIDE ONLY)
# =============================================================================
# SECURITY: These are SERVER-SIDE ONLY variables (no VITE_ prefix)
# Use these to enable creating Draft posts via the LinkedIn REST API
# If not provided, the app falls back to the LinkedIn share dialog
# Get these from LinkedIn Developer Portal with w_member_social permissions
LINKEDIN_ACCESS_TOKEN="your-linkedin-access-token"
# URN format examples:
#  - Person: urn:li:person:YOUR_PERSON_ID
#  - Organization: urn:li:organization:YOUR_ORG_ID
LINKEDIN_AUTHOR_URN="urn:li:person:your-linkedin-person-id"

# =============================================================================
# FIRECRAWL API (PREMIUM CONTENT EXTRACTION)
# =============================================================================
# API key from https://www.firecrawl.dev/
# Required for premium content extraction (JavaScript rendering, PDFs)
FIRECRAWL_API_KEY="fc-your-firecrawl-api-key-here"

# =============================================================================
# FEATURE FLAGS (A/B TESTING & ROLLOUTS)
# =============================================================================
# Enable new UX design (default: false)
VITE_ENABLE_NEW_UX=false
# Enable mobile bottom sheet for saved posts (default: false)
VITE_ENABLE_MOBILE_BOTTOM_SHEET=false
# Enable platform-specific animations (default: false)
VITE_ENABLE_PLATFORM_ANIMATIONS=false
# Enable premium features UI (default: false)
VITE_ENABLE_PREMIUM_FEATURES=false

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
# Environment (development, production, test)
NODE_ENV=development

# =============================================================================
# OPTIONAL: ANALYTICS & MONITORING
# =============================================================================
# Add your analytics tracking IDs here
# VITE_GA_TRACKING_ID="G-XXXXXXXXXX"
# VITE_PLAUSIBLE_DOMAIN="yourdomain.com"
# VITE_POSTHOG_KEY="phc_your-posthog-key"

# =============================================================================
# OPTIONAL: EMAIL CONFIGURATION
# =============================================================================
# For email notifications and transactional emails
# RESEND_API_KEY="re_your-resend-api-key"
# FROM_EMAIL="noreply@yourdomain.com"

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in all the required values above
# 3. Create accounts for the services you need:
#    - Supabase: https://supabase.com/ (Database & Auth)
#    - Anthropic: https://console.anthropic.com/ (AI Content Generation)
#    - Stripe: https://stripe.com/ (Payments)
#    - LinkedIn: https://developer.linkedin.com/ (Optional API access)
#    - Firecrawl: https://www.firecrawl.dev/ (Premium content extraction)
# 4. Set up database tables using /supabase/migrations/
# 5. Configure Stripe webhooks pointing to /api/stripe-webhook
# 6. Run: npm install && npm run dev
# 7. Visit: http://localhost:5173

# =============================================================================
# SECURITY BEST PRACTICES
# =============================================================================
# - Never commit your .env file to version control
# - Use different API keys for development and production
# - Regularly rotate your API keys
# - Set up proper CORS and rate limiting for production
# - Use Vercel environment variables for production secrets
# - Enable 2FA on all service accounts
# - Monitor API usage and set up alerts for unusual activity